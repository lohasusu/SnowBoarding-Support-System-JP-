# Web 介面
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.4
python-multipart>=0.0.9
aiofiles>=23.2.1

# 雪票爬蟲（HTTP 版，用於生產環境）
httpx>=0.27.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
deep-translator>=1.11.0

# 雪票爬蟲（Playwright 版，僅本地 CLI 使用）
playwright>=1.40.0

# 機票查詢
fast-flights>=2.2
python-dotenv>=1.0.0
requests>=2.31.0

# 會員認證
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
# 沒有新增套件：resend 用 httpx 直接呼叫 API，Google OAuth 用 httpx + 手動實作
